name: dopaz
description: "topaz.dev のプロジェクトを縦スワイプで発見するアプリ"
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.9.0

dependencies:
  cached_network_image: ^3.4.1
  device_preview: ^1.3.1
  flutter:
    sdk: flutter
  flutter_cache_manager: ^3.4.2
  font_awesome_flutter: ^11.0.0
  http: ^1.6.0
  url_launcher: ^6.3.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  assets:
    # Webプレビュー用のサンプルデータ (実APIのレスポンスをそのまま保存したもの)
    - assets/mock_projects.json
